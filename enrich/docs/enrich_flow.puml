@startuml
title Enrich Description RDF CSV

start
:Read input CSV;

if (Header contains rdf?) then (yes)
  while (Rows remain?) is (yes)
    :Parse project Turtle subset;
    :Collect labels, roots, and triples;
    :Select one source entity;
    :Resolve it by canonical ID or structural centrality;
    :Generate entity and predicate questions with SELECT SPARQL;
    :Generate directed ID question and SPARQL when available;
    :Execute and validate label SPARQL with RDFLib;
    :Execute and validate ID SPARQL with RDFLib when available;
    if (All generated queries are valid?) then (yes)
      :Create label-based question row;
      :Create directed ID row when available;
    else (no)
      :Raise validation error;
      stop
    endif
  endwhile (no)

  :Write label SPARQL CSV;
  :Write ID SPARQL CSV;
else (no)
  :Raise validation error;
endif

stop
@enduml
